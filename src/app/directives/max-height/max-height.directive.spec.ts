import { MaxHeightDirective } from './max-height.directive';

describe('MaxHeightDirective', () => {
  it('should create an instance', () => {
    const directive = new MaxHeightDirective();
    expect(directive).toBeTruthy();
  });
});
